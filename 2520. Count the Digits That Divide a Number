class Solution {
public:
    int countDigits(int num) {
       int temp=num;
       int x=0;
       int count=0;
       while(temp!=0){
        x=temp%10;
        temp=temp/10;
        if(num%x==0){
            count++;
        }
       } 
       return count;
    }
};
